# human-writer skill — Python dependencies
#
# pyyaml is REQUIRED: scripts/analyze.py loads scripts/rules.yaml via
# yaml.safe_load() at startup. Missing pyyaml is a hard failure.
#
# httpx is OPTIONAL: only the --external dispatch path in scripts/analyze.py
# (Copyleaks / GPTZero / Originality.ai detectors) imports it, lazily inside
# call sites. A missing httpx install raises ImportError which the dispatcher
# catches cleanly — the analyzer keeps working without external detectors.
#
# Install everything at user level:
#   pip install --user -r requirements.txt
#
# Or omit httpx if you'll never use --external:
#   pip install --user pyyaml

pyyaml>=6.0
httpx>=0.27  # optional, only for --external mode
